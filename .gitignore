# 运行后生成的个人配置备份 —— 含本机配置内容，不入库
backups/
*.bak

# 从本机应用提取的官方资源（仅本地留档，程序运行不依赖）
resources/

# 诊断报告 —— 由菜单生成，含本机路径与代理设置，不入库
诊断报告-*.txt

# 交接 / 维护文档 —— 本地维护用（含本机实测细节），默认不入库；
# 若希望公开，删掉下面这行再 git add 即可。
交接文档-*.md

# 临时 / 调试产物
_*
*.tmp
*.log

# 开发全记录（本地维护，不入库）
docs/

# 本地工作记忆 / 会话产物（本机笔记，非项目内容，勿提交到公开仓库）
.workbuddy/
